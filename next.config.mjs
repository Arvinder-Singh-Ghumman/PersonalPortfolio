/** @type {import('next').NextConfig} */
const nextConfig = {
  // basePath: "/PersonalPortfolio",
  // output: "export",
  // images: {
  //   unoptimized: true,
  // }
};

export default nextConfig;
